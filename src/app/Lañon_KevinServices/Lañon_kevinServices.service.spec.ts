/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Lañon_kevinServicesService } from './Lañon_kevinServices.service';

describe('Service: Lañon_kevinServices', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Lañon_kevinServicesService]
    });
  });

  it('should ...', inject([Lañon_kevinServicesService], (service: Lañon_kevinServicesService) => {
    expect(service).toBeTruthy();
  }));
});
